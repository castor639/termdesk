services:
  desktop:
    build: .
    image: termdesk-desktop
    container_name: termdesk-desktop
    ports:
      - "127.0.0.1:5900:5900"
    environment:
      GEOMETRY: 1280x800
    shm_size: 512m
    restart: unless-stopped
